import "package:flutter/material.dart";

class PinMapWidget extends StatelessWidget {
  const PinMapWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
